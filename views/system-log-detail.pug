table(class="table table-bordered table-hover table-header-bg")
  thead(class="table-header-bg thead")
    tr
      th Module Name
      th Table Name
      th Operation Mode
      th Operation Type
      th Log Type
      th Log Detail
      th Status Code
      th Created Date

  tbody
    each data, i in logInfo          
      tr
        td= data.module_name
        td= data.table_name
        td= data.operation_mode
        td= data.operation_type
        td= data.log_type
        td= data.message
        td= data.status_code
        td= data.created_date
